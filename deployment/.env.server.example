# =============================================================================
# Environment Variables for Production Server
# =============================================================================
# This file should be placed at: /home/tschaack/app-stack/.env
# It configures all services in the app-stack (n8n, OpenWebUI, Django, etc.)
#
# SECURITY: Never commit this file or share these values publicly!
# =============================================================================

# --- Database Configuration (Shared PostgreSQL) ---
# Used by: django, and potentially other apps
DB_NAME=taylorlearnsblog
DB_USER=taylorlearns
# Generate secure password: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DB_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# --- Django Configuration ---
# Generate secure secret (50+ characters):
# python3 -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET=REPLACE_WITH_SECURE_SECRET_KEY

# Debug mode (MUST be False in production)
DJANGO_DEBUG=False

# Allowed hosts (domains Django will respond to)
ALLOWED_HOSTS=taylorlearns.com,www.taylorlearns.com

# CSRF trusted origins (must include https:// protocol)
CSRF_TRUSTED_ORIGINS=https://taylorlearns.com,https://www.taylorlearns.com

# --- n8n Configuration ---
TZ=America/New_York
WEBHOOK_URL=https://n8n.taylorlearns.com
N8N_EDITOR_BASE_URL=https://n8n.taylorlearns.com
N8N_HOST=n8n.taylorlearns.com

# --- OpenWebUI Configuration ---
OLLAMA_BASE_URL=https://ollama.com

# --- Optional Services (Django) ---
# Cloudflare API (for cache purging, DNS management)
CLOUDFLARE_EMAIL=
CLOUDFLARE_TOKEN=
CLOUDFLARE_ZONE_ID=

# Sentry (error tracking)
SENTRY_DSN=

# Screenshot service (if using)
SCREENSHOT_SECRET=
